import 'package:flutter/material.dart';
class TesrScreen extends StatelessWidget {
  const TesrScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Test"),backgroundColor: Colors.yellow,),
      body: Scaffold(),
    );
  }
}
